# ===================================================================
# Backend Environment Variables
# ===================================================================
# คัดลอกไฟล์นี้เป็น .env และกำหนดค่าที่ถูกต้อง
# ===================================================================

# Database Connection
DATABASE_URL=postgresql://user:password@localhost:5432/meeting_room_db

# JWT Secret (ต้องเปลี่ยนเป็น random string ที่ปลอดภัย)
# สร้างด้วย: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=8000
NODE_ENV=development

# Frontend URL (สำหรับ CORS)
FRONTEND_URL=http://localhost:3580

# Email Configuration (Gmail SMTP)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password
FROM_EMAIL_NAME=Meeting Room Booking System

# Email Reset Password Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-app-password
EMAIL_FROM=your-email@gmail.com
